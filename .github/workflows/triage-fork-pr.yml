name: "Triage: Fork PR"
on:
  workflow_run:
    workflows: ["Checks"]
    types: [completed]
jobs:
  triage:
    uses: sdkman/.github/.github/workflows/reusable-triage-fork-pr.yml@main
    secrets: inherit
